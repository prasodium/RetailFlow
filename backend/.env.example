PORT=4000

DATABASE_URL=postgresql://postgres:password@localhost:5432/retailflow

JWT_STAFF_SECRET=change_this_in_production
JWT_CUSTOMER_SECRET=change_this_in_production

# Optional — AI recommendation endpoints fall back to non-AI suggestions when unset
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
